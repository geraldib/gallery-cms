{% extends 'admin/index.html.twig' %}

{% block content %}
    <h1>Edit Post</h1>

    {{ include('post/_form.html.twig', {'button_label': 'Update'}) }}

    <a class="btn btn-dark mt-3" href="{{ path('post_index') }}">back to list</a>

    {{ include('post/_delete_form.html.twig') }}

{% endblock %}
